<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.janita.project.dao.RoleDAO">

    <select id="getRoleListByRoleIdList" resultMap="BaseResultMap" parameterType="com.janita.project.entity.Role">
        SELECT *
        FROM role
        WHERE role_id
        IN

        <foreach item="item" index="index" collection="clazzIdList" open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>

</mapper>